# see https://symfony.com/doc/current/reference/configuration/framework.html
framework:
    secret: '%env(APP_SECRET)%'

    # The interface is German throughout. symfony/translation is not installed,
    # so this mainly settles locale-dependent behaviour (the slugger's
    # transliteration, for one) — user-facing texts are written out in German
    # where they appear.
    default_locale: de

    # Note that the session will be started ONLY if you read or write from it.
    session: true

    #esi: true
    #fragments: true

when@test:
    framework:
        test: true
        session:
            storage_factory_id: session.storage.factory.mock_file
