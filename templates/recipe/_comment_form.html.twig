{{ form_start(form, {'action': path('app_recipe_comment_new', {'id': recipe.id}), 'attr': {'id': 'comment-form', 'class': 'space-y-4'} }) }}
    {{ form_errors(form) }}

    <div>
        {{ form_label(form.text, 'Kommentar eingeben') }}
        {{ form_widget(form.text, {'attr': {'rows': 4, 'placeholder': 'Dein Kommentar …'}}) }}
        {{ form_errors(form.text) }}
    </div>

    {{ form_rest(form) }}

    <button type="submit" class="btn btn-primary">Kommentar senden</button>
{{ form_end(form) }}
