{% extends 'base.html.twig' %}

{% block title %}Erstelle ein neues Rezept{% endblock %}

{% block body %}
<div class="flex items-center justify-center p-4">
    <div class="max-w-3xl w-full bg-white rounded-xl shadow-lg p-8">
        <h1 class="text-2xl font-bold text-gray-900 mb-6 text-center">Erstelle ein neues Rezept!</h1>

        {{ include('recipe/_form.html.twig') }}

        <div class="mt-6 text-center text-sm text-gray-600">
            <a href="{{ path('app_recipe_index') }}" class="text-pinegreen hover:text-pinegreen-40 font-medium">Zurück</a>
        </div>
    </div>
</div>
{% endblock %}
