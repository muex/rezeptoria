{% extends 'base.html.twig' %}

{% block title %}Rezept bearbeiten{% endblock %}

{% block body %}
<div class="flex items-center justify-center p-4">
    <div class="max-w-3xl w-full bg-white rounded-xl shadow-lg p-8">
        <h1 class="text-2xl font-bold text-gray-900 mb-6 text-center">Rezept bearbeiten</h1>

        {{ include('recipe/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="mt-6 text-center text-sm text-gray-600">
            <a href="{{ path('app_recipe_index') }}" class="text-pinegreen hover:text-pinegreen-40 font-medium">Zurück</a>
        </div>

        {{ include('recipe/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
